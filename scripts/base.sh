#!/bin/bash

# source will be include and execute the script
source "/scripts/base_log.sh"


basename=$(basename ${0})
dirname=$(dirname ${0})

# my_dir="$(dirname "$0")"

# "$my_dir/other_script.sh"
